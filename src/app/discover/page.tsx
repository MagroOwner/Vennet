import Link from "next/link";
import { ListingCard } from "@/components/ListingCard";
import { COLLECTIONS } from "@/lib/collections";
import { getActiveListings, getFeaturedCreators } from "@/lib/queries";

export const dynamic = "force-dynamic";
export const metadata = { title: "Discover digital creators | Vennet", description: "Featured digital creators, trending tools, and original work on Vennet." };

export default async function DiscoverPage() {
  const [listings, creators] = await Promise.all([getActiveListings(undefined, 24), getFeaturedCreators(6)]);
  const trending = [...listings].sort((a, b) => b.purchaseCount - a.purchaseCount || b.viewCount - a.viewCount).slice(0, 6);
  return <main className="space-y-10 pb-12"><section className="relative overflow-hidden rounded-3xl bg-slate-950 px-7 py-12 text-white shadow-2xl shadow-slate-900/20 sm:px-10"><div className="absolute -right-20 -top-20 h-72 w-72 rounded-full bg-emerald-400/20 blur-3xl" /><div className="relative max-w-2xl"><p className="text-xs font-black uppercase tracking-[.2em] text-emerald-300">Fresh from Vennet</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">Digital work with real momentum.</h1><p className="mt-4 text-base leading-7 text-slate-300">Meet independent creators, discover useful digital tools, and find the work people are returning to right now.</p><Link href="/marketplace" className="mt-7 inline-flex rounded-xl bg-emerald-300 px-5 py-3 text-sm font-black text-slate-950">Explore every offer →</Link></div></section><section><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.18em] text-emerald-700">Editorial discovery</p><h2 className="mt-1 text-3xl font-black text-slate-950">Trending digital tools</h2></div><Link href="/marketplace?sort=popular" className="text-sm font-black text-emerald-800">See marketplace →</Link></div>{trending.length ? <div className="mt-5 grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{trending.map((listing) => <ListingCard key={listing.id} listing={listing} />)}</div> : <p className="mt-5 rounded-3xl bg-white p-8 text-slate-600">New creator work will appear here as it is published.</p>}</section><section><div><p className="text-xs font-black uppercase tracking-[.18em] text-emerald-700">Featured creators</p><h2 className="mt-1 text-3xl font-black text-slate-950">People making useful things</h2></div><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{creators.map((creator) => <Link key={creator.userId} href={"/identity/" + creator.userId} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-lg shadow-slate-900/5 transition hover:-translate-y-1"><div className="flex items-center gap-3">{creator.avatarUrl ? <img src={creator.avatarUrl} alt="" className="h-12 w-12 rounded-2xl object-cover" /> : <div className="grid h-12 w-12 place-items-center rounded-2xl bg-emerald-300 font-black text-slate-950">{creator.name.slice(0,1).toUpperCase()}</div>}<div><p className="font-black text-slate-950">{creator.name}</p><p className="mt-1 text-sm font-semibold text-emerald-700">{creator.reputationScore}% reputation</p></div></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-slate-600">{creator.bio || "Explore this creator’s digital storefront."}</p><span className="mt-4 inline-flex text-sm font-black text-emerald-800">Visit storefront →</span></Link>)}</div></section><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{COLLECTIONS.map((collection) => <Link key={collection.slug} href={"/collections/" + collection.slug} className="rounded-2xl border border-slate-200 bg-white p-5 font-black text-slate-950 shadow-sm transition hover:border-emerald-300 hover:text-emerald-800">{collection.name}<span className="mt-2 block text-sm font-medium text-slate-600">Explore collection →</span></Link>)}</section></main>;
}