CREATE TABLE IF NOT EXISTS "cart_items" ("user_id" uuid NOT NULL REFERENCES "users"("id") ON DELETE cascade, "listing_id" uuid NOT NULL REFERENCES "listings"("id") ON DELETE cascade, "created_at" timestamp with time zone DEFAULT now() NOT NULL);--> statement-breakpoint
CREATE UNIQUE INDEX IF NOT EXISTS "cart_items_user_listing_idx" ON "cart_items" ("user_id", "listing_id");--> statement-breakpoint
CREATE TABLE IF NOT EXISTS "notification_preferences" ("user_id" uuid PRIMARY KEY REFERENCES "users"("id") ON DELETE cascade, "price_drops" boolean DEFAULT true NOT NULL, "creator_releases" boolean DEFAULT true NOT NULL, "purchase_updates" boolean DEFAULT true NOT NULL, "product_updates" boolean DEFAULT true NOT NULL, "updated_at" timestamp with time zone DEFAULT now() NOT NULL);--> statement-breakpoint
CREATE TABLE IF NOT EXISTS "listing_view_events" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL, "listing_id" uuid NOT NULL REFERENCES "listings"("id") ON DELETE cascade, "source" text DEFAULT 'direct' NOT NULL, "created_at" timestamp with time zone DEFAULT now() NOT NULL);--> statement-breakpoint
CREATE INDEX IF NOT EXISTS "listing_view_events_listing_idx" ON "listing_view_events" ("listing_id", "created_at");